FROM {{ hostvars[groups['bastionhost'][0]]['ansible_host'] }}:{{ nexus_docker_secure_port }}/openshift-operators/redhat-operators:v1-orig
RUN cp /bundles.db /registry/bundles.db
CMD ["--database", "/registry/bundles.db"]
